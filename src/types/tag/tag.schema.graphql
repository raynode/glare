
type Tag implements Node {
  createdAt: DateTime!
  id: ID! @isUnique
  updatedAt: DateTime!

  tag: String!
}

extend type User { tags: [Tag!]! }

input CreateTagInput {
  tag: String!
}

extend type Query {
  tags: [Tag!]!
}

extend type Mutation {
  createTag(input: CreateTagInput!): Tag
}
