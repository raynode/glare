
type Article implements Node {
  createdAt: DateTime!
  id: ID! @isUnique
  updatedAt: DateTime!

  title: String!
  datetime: DateTime!
  link: String!
  content: String
}

extend type Query {
  articles: [Article!]!
}
