
type Account implements Node {
  createdAt: DateTime!
  id: ID! @isUnique
  updatedAt: DateTime!
  users: [User!]!
  amount: Float!
}

extend type User {
  accounts: [Account!]!
}

extend type Query {
  accounts: [Account!]!
}
